%h1= current_author.name
= link_to "New Article", new_article_path, :class => 'new_article'
%h2 Your articles
%ul
  - current_author.articles.each do |article|
    %li
      %h3= article.title
      %h4
        = article.comments.count
        Comments:
      %ul
        - article.comments.each do |comment|
          %li
            %p= comment.body
- 1000.times do
  %p
    All work and no play makes #{current_author.name} a bad person.
